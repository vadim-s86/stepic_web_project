server {
	listen	80 default;
	
	location ^~ /uploads/ {
		root /home/box/web/uploads
	}
	location ~* \.[a-zA-Z0-9]+ {
		root /home/box/web/public
	}
	location ~* ^.[^.]$ {                                                   
        return 404;                                                     
    }
}
